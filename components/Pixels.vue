<template>
  <!-- @TODO !!! -->
  <!--
  1. Have a global config for specifying each network & its associated pixel domain
    - C4Ads = pxxtag.com
    - FormulaX = firtag.com
  2. From ?net= param, work out what network we are on (if ?net exists in URL)
  3. Based on above, return the hash for this domain, from .env, matching the network

  if ($route?.net && $route.net === 'c4' && NUXT_ENV_PIXEL_HASH_C4) {
    return NUXT_ENV_PIXEL_HASH_C4;
  } else if ($route?.net && $route.net === 'fx' && NUXT_ENV_PIXEL_HASH_FX) {
    return NUXT_ENV_PIXEL_HASH_FX;
  }
  -->
</template>